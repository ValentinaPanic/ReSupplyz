<% @orders.each do |order|%>
<ul>
<li><%= order.delivery %> | <%= link_to 'Edit Order', edit_order_path(order),{method: 'get'} %> | 
<%= link_to "Delete Order", order_path(order), method: 'delete'%>
<br></li>
<ol>
<% order.products.each do |pro|%>
<li><%= pro.name%><br></li>

<%end%>
</ol>
</ul>
<%#byebug%>
<%end%>

